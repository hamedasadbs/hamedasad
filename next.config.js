/** @type {import('next').NextConfig} */

const nextConfig = {
  // experimental: {
  //   appDir: true,
  // },
  // distDir: "out",
  // output: "export",
  images: {
    domains: ["img.freepik.com"],
  },
};

module.exports = nextConfig;
